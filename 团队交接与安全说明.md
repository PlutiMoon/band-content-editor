# BAND 在线内容工具：团队交接与安全说明

这份文档给团队成员看，目标是两件事：会用在线编辑器，不把关键密码弄丢或传错地方。

## 1. 现在怎么进入网页

1. 打开浏览器。
2. 进入线上地址：`https://online-omega-two.vercel.app`
3. 在“项目访问密钥”里输入团队共享口令。
4. 填自己的名字。
5. 如果是自己的电脑，可以勾选“记住我”。
6. 点“进入”。

如果提示“访问口令错误或已轮换”，通常只有三种情况：

1. 输错了。
2. 负责人已经换过新口令。
3. 浏览器记住了旧口令，需要重新输入。

## 2. 这个口令到底是什么

项目访问口令相当于“编辑器门禁卡”。

它能做的事：

- 进入在线内容编辑器。
- 拉取数据库里的内容。
- 保存行动、事件、对话、手机消息、世界配置等内容。

它不能做的事：

- 不能登录 Supabase 后台。
- 不能看到 Supabase 服务端密钥。
- 不能绕过数据库后台的安全配置。

简单说：团队成员日常只需要这个访问口令，不需要 Supabase secret key。

## 3. 哪些东西不能写进群公告或仓库

下面这些不能写进 GitHub、公开文档、聊天截图、群公告：

- Supabase `secret key`
- 数据库密码
- Vercel 账号登录凭据
- GitHub 账号登录凭据
- 任何能直接管理后台或绕过网页门禁的令牌

下面这些可以给团队成员：

- 线上编辑器地址
- 项目访问口令
- 这份使用说明

建议负责人把真实敏感信息放在离线文档或密码管理器里，例如本机私有备忘：

`D:\ALLCODE\BAND_敏感信息备忘.local.md`

这个文件不要提交到 Git，也不要发到公开群里。

## 4. 日常编辑流程

每次编辑内容按这个顺序来：

1. 打开网页并登录。
2. 点左侧对应栏目，例如“行动”“事件”“对话”“手机消息”“世界”。
3. 新增或修改内容。
4. 点保存。
5. 看到绿色成功提示后，再切换页面或关闭浏览器。
6. 改完一批内容后，打开“体检”。
7. 点“重新体检”。
8. 如果有红色问题，先修红色问题。
9. 没有红色问题后，再做发布或同步。

一句话记法：先改内容，再保存，再体检，最后发布。

## 5. 怎么判断线上是否正常

普通成员只看这几项：

1. 网页能打开。
2. 口令能进入。
3. 点“从数据库拉取”能看到内容。
4. 保存后能看到成功提示。
5. 刷新网页后，刚才保存的内容还在。

如果这五项都正常，说明在线编辑器、数据库读写、口令校验基本都正常。

## 6. 口令会不会自动乱改

不会。

当前机制不是自动改密码。项目访问口令只会在负责人手动去 Supabase 后台执行轮换 SQL 时改变。

换句话说：

- 不会因为刷新网页自动变。
- 不会因为别人登录自动变。
- 不会因为保存内容自动变。
- 只有负责人手动轮换，旧口令才会失效。

## 7. 什么时候需要换项目访问口令

出现下面情况时建议换：

1. 有成员离开项目，不希望继续有编辑权限。
2. 口令发错人。
3. 口令被截图或转发到不该出现的地方。
4. 怀疑有人乱改内容。
5. 定期安全整理，例如每月一次或每个版本结束后一次。

如果只是普通成员输错密码，不需要换。

## 8. 负责人怎么轮换项目访问口令

只有负责人或有 Supabase 后台权限的人执行。

步骤：

1. 先想好一个新的长口令。
2. 打开 Supabase 项目后台。
3. 进入 SQL Editor。
4. 执行下面 SQL，把 `NEW_EDITOR_ACCESS_KEY` 换成新口令：

```sql
UPDATE public.app_settings
SET value = crypt('NEW_EDITOR_ACCESS_KEY', gen_salt('bf')),
    updated_at = NOW()
WHERE key = 'editor_access_key_hash';
```

5. 打开线上编辑器，用新口令登录测试。
6. 确认能拉取内容、能保存。
7. 把新口令更新到负责人私有备忘或密码管理器。
8. 通知团队成员旧口令已失效，改用新口令。

注意：不要把新口令写进这个仓库的文档里。

## 9. 如果团队成员进不去

按顺序排查：

1. 确认网址是不是 `https://online-omega-two.vercel.app`
2. 确认口令是不是最新的。
3. 让对方手动重新输入，不要用浏览器自动填充。
4. 换 Chrome 或 Edge 试一次。
5. 手机网络和电脑网络各试一次。
6. 如果所有人都进不去，负责人检查 Supabase 和 Vercel。
7. 如果只有一个人进不去，优先清浏览器缓存或换浏览器。

## 10. 如果保存失败

按顺序排查：

1. 看页面提示是不是“访问口令错误或已轮换”。
2. 如果是，重新登录。
3. 如果不是，点“从数据库拉取”看看能不能读到内容。
4. 打开“体检”，看有没有红色问题。
5. 复制当前正在编辑的文字到本地临时文本，避免丢内容。
6. 刷新网页后再试保存。
7. 仍然失败时联系负责人。

## 11. 负责人日常维护清单

每次大改内容后做一次：

1. 登录线上编辑器。
2. 点“从数据库拉取”。
3. 打开“体检”，点“重新体检”。
4. 打开“关系图”，看是否有断链或孤岛。
5. 打开“搜索”，搜一个刚改过的内容，确认能跳转。
6. 打开“快照”，必要时保存一份当前状态。
7. 如果准备交给游戏使用，再做同步到本地 JSON。

每次换口令后做一次：

1. 用旧口令测试，应当无法进入。
2. 用新口令测试，应当可以进入。
3. 保存一条小改动或测试已有内容能否正常保存。
4. 把新口令只更新到私有备忘或密码管理器。

## 12. 给团队的一句话版本

平时只需要记住：

1. 用负责人给的项目访问口令进网页。
2. 改完内容一定点保存。
3. 发布前一定跑体检。
4. 红色问题先修，不要硬发。
5. 服务端密钥不要问、不要传、不要写文档。
