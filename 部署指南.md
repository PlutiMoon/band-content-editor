# 在线编辑器部署指南

部署需要两个免费服务：Supabase（数据库）和 Vercel（托管）。全程约 15 分钟，只需 GitHub 账号。

---

## 第一步：创建 Supabase 项目

1. 打开 [supabase.com](https://supabase.com)，点击右上角 **Sign in with GitHub**
2. 登录后点击 **New project**
3. 填写：
   - Name：`band-editor`（或随便）
   - Database Password：生成一个复杂密码并**保存好**
   - Region：选 **Northeast Asia (Tokyo)** 或 Southeast Asia (Singapore)，离国内近
4. 点击 **Create new project**，等待 2-3 分钟数据库初始化

## 第二步：创建数据表

1. 在 Supabase 左侧菜单点击 **SQL Editor**
2. 点击 **New query**
3. 打开本目录下的 `migration.sql`，**复制全部内容**粘贴到 SQL Editor
4. 点击右下角绿色 **Run** 按钮
5. 看到 "Success" 即完成

## 第三步：获取连接凭据

1. 在 Supabase 左侧菜单点击 **Settings** → **API**
2. 找到这两个值并**复制下来**：
   - **Project URL**（类似 `https://xxxxx.supabase.co`）
   - **publishable key**（以 `sb_publishable_` 开头，给浏览器端使用）
   - **secret key**（以 `sb_secret_` 开头，只给本地同步脚本使用，不能写进前端文件或提交到仓库）
3. 用文本编辑器打开 `js/config.js`，找到最上面的配置区域，替换：
   ```javascript
   export const SUPABASE_URL = 'YOUR_SUPABASE_URL';       // 替换为 Project URL
   export const SUPABASE_ANON_KEY = 'YOUR_PUBLISHABLE_KEY'; // 替换为 publishable key
   ```

> 注意：`SUPABASE_ANON_KEY` 这个变量名沿用旧代码命名，但实际应填写 publishable key。secret key 权限高，只能放在本机环境变量 `SUPABASE_SECRET_KEY` 中供同步脚本读取。

## 第四步：部署到 Vercel

### 方式 A：通过 GitHub 部署（推荐）

1. 在项目根目录新建一个 GitHub 仓库，把 `内容工具/online/` 整个文件夹传上去
   ```bash
   # 在 代号：BAND 目录下
   git init
   git add 内容工具/online/
   git commit -m "在线内容编辑器"
   git remote add origin https://github.com/你的用户名/band-editor.git
   git push -u origin main
   ```
2. 打开 [vercel.com](https://vercel.com)，点击 **Sign in with GitHub**
3. 点击 **New Project**，选择刚才的仓库
4. 在 **Root Directory** 设置中填：`内容工具/online`
5. 其他设置不动，直接点 **Deploy**
6. 几秒后获得一个公网 URL，类似 `https://band-editor.vercel.app`

### 方式 B：命令行部署

```bash
npm i -g vercel
cd 内容工具/online/
vercel
# 按提示登录 GitHub，一路默认即可
```

## 第五步：导入现有数据

1. 打开部署后的 URL，输入 Supabase RPC 中配置的团队编辑器口令，填写你的名字
2. 点击左侧「从数据库拉取」— 此时数据库为空
3. 切换到「行动」Tab，点击「📥 导入文件」，选择 `assets/json/actions.json`
4. 切换到「事件」Tab，同样导入 `events.json`
5. 切换到「对话」Tab，逐个导入 `dialogues/` 目录下的所有 JSON 文件
6. 切换到「手机消息」Tab，导入 `phone_chat.json`
7. 每次导入后数据自动写入数据库，其他成员刷新即可看到

## 第六步：分享给团队

把公网 URL + 团队编辑器口令发给团队成员，他们打开网址输入口令即可进入。所有人实时看到相同数据。

---

## 常见问题

**Q: 需要翻墙吗？**
Supabase 和 Vercel 在中国大陆有时访问较慢但通常可用。如果打不开，可以试试 Cloudflare 加速。

**Q: 数据安全吗？**
游戏内容本身不算高度敏感，但数据库写权限仍然需要保护。浏览器端只能使用 publishable key；本地同步脚本使用的 secret key 必须通过环境变量配置，并且泄露后需要在 Supabase 后台立即轮换。

**Q: 数据库免费额度够用吗？**
Supabase 免费 tier 提供 500MB 数据库 + 50,000 月活用户。你们的 JSON 内容最多几十 KB，完全够用。

**Q: 本地版和在线版能共用吗？**
可以。在在线版中点击「导出文件」下载 JSON，复制到 `assets/json/` 覆盖本地文件，然后在 Godot 中运行。
