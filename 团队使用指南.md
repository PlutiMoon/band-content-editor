# 代号：BAND — 在线编辑器使用指南（完整版）

本指南涵盖编辑器的全部功能。不需要任何编程知识，会填表就行。

---

## 目录

- [一、怎么打开](#一怎么打开)
- [二、五个标签页分别干什么](#二五个标签页分别干什么)
- [三、⚡ 行动 — 玩家能做什么](#三-行动--玩家能做什么)
- [四、📋 事件 — 满足条件后自动发生的事](#四-事件--满足条件后自动发生的事)
- [五、💬 对话 — 角色说的话](#五-对话--角色说的话)
- [六、📱 手机消息 — 游戏里的聊天记录](#六-手机消息--游戏里的聊天记录)
- [七、🌍 世界 — 管理游戏规则和地图配置](#七-世界--管理游戏规则和地图配置)
- [八、怎么保存 & 怎么同步到游戏](#八怎么保存--怎么同步到游戏)
- [九、多人协作注意事项](#九多人协作注意事项)
- [十、参考速查表](#十参考速查表)
- [十一、常见问题](#十一常见问题)

---

## 一、怎么打开

1. 打开浏览器（Chrome、Edge 或手机浏览器都行）
2. 在地址栏输入：**`online-omega-two.vercel.app`**，回车
3. 输入访问密码（向项目负责人获取）
4. 填上你的名字（方便大家知道谁在编辑）
5. 勾选「记住我」下次打开就不用再输密码了
6. 点「进入」

进入后左侧会看到五个标签页：

```
⚡ 行动    📋 事件    💬 对话    📱 手机消息    🌍 世界
```

---

## 二、五个标签页分别干什么

| 标签页 | 管理内容 | 游戏里对应什么 |
|--------|---------|--------------|
| ⚡ 行动 | 玩家能做的事情 | 比如打工、练琴、演出 |
| 📋 事件 | 条件触发的事情 | 比如房东催租、获得奖励 |
| 💬 对话 | NPC 说的话 | 对话系统里的每一句台词 |
| 📱 手机消息 | 手机聊天记录 | 玩家手机收到的信息 |
| 🌍 世界 | 游戏规则和地图 | 一周几天、有哪些地点、有哪些 NPC |

**简单记忆：前四个管「内容」，最后一个管「规则」。**

---

## 三、⚡ 行动 — 玩家能做什么

### 基本概念

每个行动代表玩家在游戏中能做的一件事。比如「练琴」是一个行动，「打工」是另一个行动。

### 字段说明

| 字段 | 意思 | 怎么填 |
|------|------|--------|
| ID | 程序用的代号 | 英文 + 下划线，如 `practice` |
| 名称 | 游戏里显示的名字 | 中文，如「练琴」 |
| 描述 | 行动说明 | 一句话 |
| 地点 | 在哪个场景能做 | 下拉菜单选 |
| 消耗时段 | 花几个时段 | 通常填 1（一个时段约等于半天） |
| 每日上限 | 一天最多做几次 | 填数字 |

### 条件（可以不填）

条件控制"什么情况下能做这个行动"：

| 条件类型 | 怎么用 |
|---------|--------|
| 属性要求 | 比如「演奏力 >= 20」才能做 |
| 需要 flag | 需要某个标记已经触发过 |
| 不能有 flag | 某个标记存在时不能做 |
| 时段要求 | 只在某个时段能做（如「傍晚」） |
| 日期要求 | 只在第几天能做（如第 3 天） |
| 金钱要求 | 比如「金钱 >= 100」才能做 |

### 效果（可以不填）

效果控制"做完之后发生什么"：

| 效果类型 | 怎么填 |
|---------|--------|
| 属性变化 | 选属性 → 填数值（正数=增加，负数=减少） |
| 金钱变化 | 正数=得到钱，负数=花掉钱 |
| 关系变化 | 选 NPC → 填变化值 |
| 设置 flag | 设置一个标记（可以用在事件/对话的条件里） |
| 睡觉 | 勾选后推进到次日早晨 |

### 操作

- **新增**：点工具栏的「+ 新增」按钮
- **编辑**：点表格里某一行，在下方编辑
- **删除**：点行末的 ✕
- **搜索**：工具栏搜索框输入关键词过滤
- **导出**：点「📤 导出文件」下载 JSON 文件

---

## 四、📋 事件 — 满足条件后自动发生的事

### 基本概念

事件不是玩家主动触发的，而是系统检测到"条件满足"后自动发生。比如"第 4 天早晨，房东来催租"。

### 字段说明

| 字段 | 意思 | 怎么填 |
|------|------|--------|
| ID | 程序代号 | 英文 + 下划线，如 `landlord_rent` |
| 名称 | 事件名称 | 中文 |
| 触发类型 | 什么时候检查 | 下拉菜单选（见下方） |
| 一次性 | 只发生一次？ | 大部分选「是」 |

### 触发类型

| 类型 | 什么时候触发 | 举例 |
|------|-------------|------|
| 进入时段 | 每进入一个时段时检查 | 每天早晨自动发生的事 |
| 完成行动后 | 玩家完成某个行动后 | 演出后获得奖励 |
| 进入地点时 | 玩家走进某个场景时 | 进入酒吧触发事件 |
| 属性达标 | 某项属性达到阈值时 | 精神状态太低触发崩溃 |
| 一周结束时 | 第 N 天结束时 | 结算事件 |

### 条件（控制是否触发）

和行动的「条件」规则完全一样，额外多了一个：

| 额外条件 | 怎么用 |
|---------|--------|
| 日期范围 | 比如第 2~5 天之间才触发 |

### 效果

和行动的「效果」规则一样，额外多了一个：

| 额外效果 | 怎么用 |
|---------|--------|
| 触发对话 | 选择要播放的对话 ID，事件发生时会自动弹出对话 |

---

## 五、💬 对话 — 角色说的话

### 基本概念

每个对话是一个"对话树"——从开头（start）开始，经过一系列 NPC 说话和玩家选择，最终到达结束（end）。

```
start → NPC说「你来啦」→ 玩家选「聊聊」→ NPC说「好啊」→ end
                      → 玩家选「算了」→ NPC说「再见」→ end
```

### 节点字段

| 字段 | 意思 | 怎么填 |
|------|------|--------|
| 节点 ID | 节点的标签 | `start`=开头，`end`=结束，中间节点自己取名 |
| 说话人 | 这句话谁说的 | NPC 名字 / 「旁白」（叙述）/ 「系统」（提示） |
| 文本 | 说的话 | 正文内容 |
| 下一节点 | 说完跳到哪 | 填节点 ID（和"玩家选项"二选一） |
| 玩家选项 | 给玩家的选择 | 每个选项：文字 + 跳转目标 |
| 效果 | 这句话产生的效果 | 和行动的效果规则一致 |
| 表情 | 说话时的表情 | 默认「neutral」 |

### 规则

- **必须有 start 和 end 节点**
- end 节点的文本可以留空
- 「下一节点」和「玩家选项」只能二选一
- 如果都不填，对话就卡住了

### 操作

- **新增对话树**：点「+ 新增对话」
- **新增节点**：在对话树中新增节点
- **删除整棵对话树**：点对话列表中的 ✕

---

## 六、📱 手机消息 — 游戏里的聊天记录

### 基本概念

每个"会话"（chat）是一个聊天窗口。可以是私聊（type: private）或群聊（type: group）。

每条消息需要指定**什么时候发送**：

| 发送方式 | 什么时候发 | 怎么填 |
|---------|-----------|--------|
| 定时发送 | 到第 X 天第 Y 个时段 | 填 delay_day + delay_phase |
| 事件触发 | 某事件发生后发 | 填 trigger_event（事件 ID） |

两种方式二选一，不能同时填。

### 消息字段

| 字段 | 意思 |
|------|------|
| sender | 发送者显示的名字 |
| text | 消息正文 |

### 操作

- **新增会话**：点「+ 新增」
- **编辑会话信息**：点会话行，修改名称和类型
- **添加消息**：在右侧消息区域操作
- **手机端**：同样支持，点击会话卡片进入消息编辑

---

## 七、🌍 世界 — 管理游戏规则和地图配置

### 基本概念

这个标签页控制的是**游戏本身的规则**，而不是内容。分成三个分页：

### ⚙ 游戏参数

控制游戏的数值规则。改完保存后，游戏就会按新规则运行。

| 字段 | 意思 | 默认值 |
|------|------|--------|
| 一周天数 | 游戏里一周有几天 | 7 |
| 初始金钱 | 玩家新游戏开始的金钱 | 500 |
| 属性下限 | 属性最低能到多少 | 0 |
| 属性上限 | 属性最高能到多少 | 100 |
| 初始地点 | 玩家出生在哪个地点 | 出租屋 |
| 初始 X/Y 坐标 | 玩家在地图上的出生位置 | (100, 240) |
| 初始属性值 | 6 个属性的初始数值 | 演奏力 15、创作力 20... |

**用途示例：**

- 想把游戏改成 3 天周期 → 改「一周天数」为 3
- 想提高游戏难度 → 降低「初始金钱」和「初始属性值」
- 想扩展属性范围 → 改「属性上限」

### 📍 地点

管理游戏中的所有地点（场景）。表格中每行是一个地点。

| 字段 | 意思 |
|------|------|
| ID | 程序代号（英文下划线） |
| 名称 | 游戏里显示的名字 |
| 场景路径 | Godot 场景文件路径（程序配置，一般不改） |
| 触发 X/Y | 玩家走到哪个坐标时弹出"按 E 进入" |
| 建筑 X/Y/W/H | 地图上建筑方块的位置和大小 |
| 标签 X/Y | 建筑名字显示在地图上的位置 |

**操作：**
- **新增地点**：点「+ 新增地点」 → 输入 ID → 在表格里点击编辑详情
- **搜索**：工具栏搜索框输入关键词过滤
- **删除**：点行末 ✕
- **导入/导出**：可以导入已有的 locations.json 文件

添加新地点后，它会自动出现在「行动」和「事件」标签页的地点下拉菜单中，不需要额外配置。

### 👤 NPC

管理游戏中的所有角色。

| 字段 | 意思 |
|------|------|
| ID | 程序代号（如 `npc_xiao_ming`） |
| 名称 | 游戏里显示的名字（如「小明」） |
| 对话 ID | 关联的对话文件名（如 `npc_小明`） |
| 地图 X/Y | NPC 在地图上的位置 |

**重要：** 添加新 NPC 后，别忘了给他创建对应的对话文件（在「💬 对话」标签页里），否则游戏里这个 NPC 会没有对话。

添加新 NPC 后，它会自动出现在「行动」和「事件」标签页的关系系统下拉菜单中。

---

## 八、怎么保存 & 怎么同步到游戏

### 保存

**关键：改完一定要点「💾 保存到数据库」按钮。点之前修改不会生效。**

- 保存成功 → 左下角弹出绿色提示「已保存」
- 保存失败 → 弹出红色提示，检查网络后重试

### 数据流

```
你在编辑器里填表 → 点保存 → 存到云端数据库
                                    ↓
                        运行「一键同步.bat」
                                    ↓
                        游戏 JSON 文件更新
                                    ↓
                        打开 Godot 运行游戏
                                    ↓
                        你在编辑器里填的内容出现在游戏中
```

### 如何同步

方式一：双击项目目录下的 **`一键同步.bat`**

方式二：在命令行运行：
```
node 内容工具/sync_to_game.js
```

同步完成后会显示变更摘要，比如：
```
📊 变更摘要:
  行动  8(±0)
  事件  8(±0)
  对话  13(±0)
  手机  4(±0)
  地点  5(±0)
  NPC   5(±0)
  游戏配置  未变化
```

---

## 九、多人协作注意事项

### 谁在线

左侧「在线成员」区域显示当前在编辑器里的人。手机上点底部的圆点可以看到名字。

### 修改冲突

- 如果两个人**同时编辑不同内容**——互不影响
- 如果两个人**同时编辑同一个行动/事件**——后保存的覆盖先保存的
- 其他人在编辑时你会收到提示「📡 XXX 更新了…」
- 如果你正在编辑表单时收到这个提示，建议先保存你的修改，再点「🔄 从数据库拉取」看最新内容

### 切换标签页

如果你正在编辑一个表单（详情面板打开着），切换到其他标签页时会弹出提醒「你有未保存的编辑」。这是为了防止你忘了保存。

---

## 十、参考速查表

### 属性（6 个）

| 属性名 |
|--------|
| 演奏力 |
| 创作力 |
| 社交力 |
| 魅力 |
| 执行力 |
| 精神状态 |

### 时段（4 个）

早晨 → 下午 → 傍晚 → 深夜

### 运算符

| 符号 | 意思 |
|------|------|
| >= | 大于等于 |
| <= | 小于等于 |
| > | 大于 |
| < | 小于 |
| == | 等于 |
| != | 不等于 |

### 触发类型（5 种）

| 类型 | 意思 |
|------|------|
| phase_start | 进入时段 |
| action_complete | 完成行动后 |
| location | 进入地点时 |
| stat_threshold | 属性达标 |
| week_end | 一周结束时 |

---

## 十一、常见问题

**Q: 我不小心关了页面，内容还在吗？**

只要点了「💾 保存到数据库」就在。没点保存的话会丢失。

**Q: 怎么删除一个行动 / 事件 / 对话 / 地点 / NPC？**

每行右边有红色的 ✕ 按钮，点两下（确认后）就删了。

**Q: 怎么删除整棵对话树？**

在「💬 对话」标签页的对话列表中，每棵对话树右边有 ✕ 删除按钮。

**Q: 网页打不开怎么办？**

检查网络是否正常。如果长时间打不开，联系项目负责人。

**Q: 手机能用吗？**

能。浏览器打开同一个网址就行，界面会自动适配手机屏幕。

**Q: 我在手机上怎么切换标签页？**

页面底部有标签页选择框，点开就能切换。

**Q: 我该填什么内容？**

内容设计由策划/文案负责。本工具只负责数据录入和配置。

**Q: 新增了一个 NPC，为什么在行动编辑器里选不到？**

新增 NPC 后需要先保存。保存后刷新页面或切换一下标签页，下拉菜单就会更新。

**Q: 新增了一个地点，玩家为什么进不去？**

除了在编辑器里添加地点，还需要程序员在 Godot 里创建对应的场景文件（.tscn）。编辑器只管理数据，场景的视觉和美术资源需要单独制作。

**Q: 改了一周天数，游戏里为什么没变？**

修改 → 保存 → 运行「一键同步.bat」→ 重新启动 Godot 游戏。三步缺一不可。
