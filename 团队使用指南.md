# 代号：BAND — 在线编辑器使用指南（完整版）

本指南涵盖编辑器的全部功能。不需要任何编程知识，会填表就行。每一步都有详细说明。

---

## 目录

- [一、怎么打开](#一怎么打开)
- [二、五个标签页分别干什么](#二五个标签页分别干什么)
- [三、⚡ 行动 — 玩家能做什么](#三-行动--玩家能做什么)
- [四、📋 事件 — 条件触发的事](#四-事件--条件触发的事)
- [五、💬 对话 — 角色说的话](#五-对话--角色说的话)
- [六、📱 手机消息 — 游戏里的聊天](#六-手机消息--游戏里的聊天)
- [七、🌍 世界 — 游戏规则与配置](#七-世界--游戏规则与配置)
	- [⚙ 游戏参数](#-游戏参数)
	- [🗺 地图](#-地图)
	- [📍 地点](#-地点)
	- [👤 NPC](#-npc)
- [八、怎么保存 & 怎么同步到游戏](#八怎么保存--怎么同步到游戏)
- [九、多人协作注意事项](#九多人协作注意事项)
- [十、参考速查表](#十参考速查表)
- [十一、常见问题](#十一常见问题)
- [十二、完整操作流程示例](#十二完整操作流程示例)

---

## 一、怎么打开

1. 打开浏览器（Chrome、Edge、手机浏览器都可以）
2. 在地址栏输入：**`online-omega-two.vercel.app`**，按回车
3. 输入访问密码（向项目负责人获取）
4. 填上你的名字——方便其他人知道谁在编辑
5. 勾选「**记住我**」——下次打开不用重新输密码
6. 点「**进入**」

> **提示**：如果打不开，先检查网络是否正常。长时间无法访问请联系项目负责人。

进入后你会看到左侧五个标签页按钮和右侧的编辑区域：

```
┌─ 侧边栏 ───────────────────────┐  ┌─ 编辑区域 ───────────────┐
│                                 │  │                          │
│  ⚡ 行动                        │  │  工具栏（搜索/新增/导入）   │
│  📋 事件                        │  │                          │
│  💬 对话                        │  │  内容表格                │
│  📱 手机消息                    │  │                          │
│  🌍 世界                        │  │  编辑面板（点行展开）      │
│                                 │  │                          │
│  [在线成员列表]                  │  │                          │
│  [🔄 从数据库拉取]              │  │                          │
│  [📦 全部导出]                  │  │                          │
└─────────────────────────────────┘  └──────────────────────────┘
```

---

## 二、五个标签页分别干什么

| 标签页 | 管理什么 | 游戏里的表现 |
|--------|---------|------------|
| ⚡ **行动** | 玩家能做什么事 | 练琴、打工、演出、社交…… |
| 📋 **事件** | 满足条件自动发生的事 | 第 4 天房东催租、演出后获得奖励…… |
| 💬 **对话** | 每个 NPC 说什么话 | 跟小明聊天、跟老王讨价还价…… |
| 📱 **手机消息** | 玩家收到的聊天信息 | 群聊消息、NPC 私聊…… |
| 🌍 **世界** | 游戏本身的规则 | 一周几天、有哪些地图、有哪些地点和 NPC |

**简单记法**：前四个管「游戏里有什么内容」，最后一个管「游戏怎么运行」。

---

## 三、⚡ 行动 — 玩家能做什么

### 什么是行动

每个行动 = 玩家在游戏中能做的一件事。

比如「练琴」是一个行动，玩家在排练室点一下就能执行。「打工」是另一个行动，在 Livehouse 点一下就能执行。

### 字段说明

打开行动标签页后，你会看到一个表格。每一行是一个行动。以下是每个字段的含义：

| 字段 | 意思 | 怎么填 | 举例 |
|------|------|--------|------|
| ID | 程序用的代号，**不能有中文** | 英文小写 + 下划线 | `practice`、`work_livehouse` |
| 名称 | 游戏里显示的名字 | 中文 | `练琴`、`打工` |
| 描述 | 行动的一句话说明 | 随便写 | `在排练室练习演奏技巧` |
| 地点 | 在哪个场景能做 | 下拉菜单选 | `排练室` |
| 消耗时段 | 花几个时段（半天=1个时段） | 数字，通常填 1 | `1`（睡觉填 `0`） |
| 每日上限 | 一天最多做几次 | 数字 | `3`（不限制可以不填） |

### 条件（可以不填）

条件 = "什么情况下这个行动才显示/可点击"。

**怎么操作**：点表格中的某一行，下方会展开编辑面板。在「条件」区域点 **+ 添加** 按钮来增加条件。不需要条件的话全部留空即可。

| 条件类型 | 意思 | 怎么操作 |
|---------|------|---------|
| 属性要求 | 某项属性达标才能做 | 点「+ 添加」（stat 行），选属性 → 选运算符 → 填数值 |
| 需要 flag | 某个事件触发过才能做 | 点「+ 添加」（flag 行），填标记名 |
| 不能有 flag | 某个事件触发过就**不能**做 | 同上，填标记名 |
| 时段要求 | 只在特定时段能做 | 下拉菜单选，比如选「傍晚」 |
| 日期要求 | 只在第几天能做 | 填数字，比如 `3`（第 3 天） |
| 金钱要求 | 钱够了才能做 | 选运算符 + 填金额 |

**举例**：如果想让「演出」这个行动要求演奏力 ≥ 20、金钱 ≥ 100：
- 点属性要求的「+ 添加」→ 选"演奏力" → 选">=" → 填"20"
- 点金钱要求 → 选">=" → 填"100"

### 效果（可以不填）

效果 = "做完这个行动后发生什么"。

| 效果类型 | 意思 | 怎么操作 |
|---------|------|---------|
| 属性变化 | 某项属性增加/减少 | 点「+ 添加」，选属性，填变化值（正数=加，负数=减） |
| 金钱变化 | 金钱增加/减少 | 填数字，正数=得到钱，负数=花掉钱 |
| 关系变化 | 跟某个 NPC 关系变好/变差 | 点「+ 添加」，选 NPC，填变化值 |
| 设置 flag | 留下一个标记 | 点「+ 添加」，填标记名 |
| 睡觉 | 推进到第二天早晨 | 勾选 ☑ 即可 |

**举例**：「打工」的效果——金钱 +200、演奏力 +5、精神状态 -10：
- 金钱变化：填 `200`
- 属性变化：添加两行——演奏力 +5、精神状态 -10

### 详细操作步骤

**新增一个行动**：
1. 确保你在「⚡ 行动」标签页
2. 点工具栏的「**+ 新增**」按钮
3. 在弹出的输入框里填行动 ID（英文下划线），点确定
4. 表格最下面会多出一行，自动展开编辑面板
5. 填写名称、选择地点、填写消耗时段等
6. 如果需要条件或效果，在对应区域点「+ 添加」
7. 点最下面的「**💾 保存到数据库**」
8. 看到绿色提示「已保存」说明成功了

**编辑已有行动**：
1. 点表格中你要编辑的那一行（整行都会高亮）
2. 表格下方自动展开编辑面板
3. 修改你想改的字段
4. 点「**💾 保存到数据库**」
5. 如果不想改了，点「**取消**」按钮回到列表

**删除一个行动**：
1. 点表格中那一行最右边的 **✕** 按钮
2. 弹出确认框，点「确定」
3. 数据会自动保存到数据库

**搜索行动**：
1. 在工具栏的搜索框里输入关键词
2. 表格会实时过滤，只显示匹配的行
3. 清空搜索框恢复完整列表

**导出/导入**：
- 点「**📤 导出文件**」下载 `actions.json`，可以备份
- 点「**📥 导入文件**」选择 `.json` 文件批量导入

---

## 四、📋 事件 — 条件触发的事

### 什么是事件

事件不是玩家主动触发的，而是游戏系统在后台自动检测的。当"触发条件"满足时，事件自动执行。

比如你在策划文档里写："第 4 天早晨，房东来催租"——这就是一个事件。

### 字段说明

| 字段 | 意思 | 怎么填 |
|------|------|--------|
| ID | 程序用的代号 | 英文 + 下划线，如 `landlord_rent` |
| 名称 | 事件名称 | 中文，如 `房东催租` |
| 触发类型 | 什么时候检测这个事件 | 下拉菜单选（见下表） |
| 一次性 | 只发生一次？ | 大部分选「是」（勾选一次性的勾选框） |

### 触发类型详解

| 类型 | 什么时候检测 | 什么时候用 |
|------|------------|----------|
| **进入时段** | 每进入早晨/下午/傍晚/深夜时 | 固定时间发生的事，如"每天早晨自动提醒" |
| **完成行动后** | 玩家做完某个特定行动后 | 行动引发的后续，如"演出结束后粉丝送花" |
| **进入地点时** | 玩家走进某个场景时 | 场景触发的事，如"进入酒吧碰到熟人" |
| **属性达标** | 某项属性达到某个值后 | 状态触发，如"精神状态太低触发崩溃事件" |
| **一周结束时** | 一周结束后 | 结算类事件 |

> **注意**：选了「完成行动后」的话，需要在触发详情里填具体是哪个行动（填行动 ID）。

### 条件（控制是否触发）

和行动的条件规则一样，额外多了一个：

| 条件 | 意思 | 怎么填 |
|------|------|--------|
| 日期范围 | 在哪几天之间才触发 | 填开始日 + 结束日，如第 2~5 天 |

### 效果

和行动的效果规则一样，额外多了一个：

| 效果 | 意思 | 怎么填 |
|------|------|--------|
| 触发对话 | 事件发生时自动播放一段对话 | 填对话 ID（在「💬 对话」标签页里能看到所有对话 ID） |

### 详细操作步骤

和行动完全一样——点击行编辑、+ 新增、✕ 删除、搜索过滤、导入导出。

**典型示例**：「第 4 天早晨，房东催租」

1. 点「+ 新增」，输入 ID：`landlord_rent`
2. 名称填：`房东催租`
3. 触发类型选：`进入时段`
4. 一次性：勾选
5. 条件 → 日期要求填 `4`，时段要求选 `早晨`
6. 效果 → 触发对话填 `event_landlord`
7. 点「💾 保存到数据库」

---

## 五、💬 对话 — 角色说的话

### 什么是对话树

对话是树状结构的——从开头（start）开始，NPC 说话 → 玩家做选择 → 不同的选择走向不同的分支 → 最终到达结束（end）。

```
start
  │
  ▼
NPC：「你来啦，想聊什么？」
  │
  ├─ 玩家选「聊聊最近」→ NPC：「最近啊……」→ end
  │
  └─ 玩家选「没什么事」→ NPC：「好吧」→ end
```

### 界面布局

对话标签页是左右分栏的：

```
┌─ 左侧：对话列表 ─────┐  ┌─ 右侧：节点编辑区 ────────────┐
│                      │  │                                │
│  npc_小明    [✕]     │  │  节点列表（表格）               │
│  npc_老王    [✕]     │  │  ├─ start  [编辑]              │
│  event_landlord [✕]  │  │  ├─ node_1 [编辑]              │
│                      │  │  └─ end                        │
│  [+ 新增对话]        │  │                                │
│                      │  │  编辑面板（点编辑展开）          │
└──────────────────────┘  └────────────────────────────────┘
```

### 节点字段说明

每个节点 = 对话中的一句话或一个选择。

| 字段 | 意思 | 怎么填 |
|------|------|--------|
| 节点 ID | 这个节点的标签 | `start` = 开头，`end` = 结束，中间节点自己取名 |
| 说话人 | 这句话谁说的 | NPC 名字 / `旁白`（故事叙述）/ `系统`（系统提示） |
| 文本 | 说的话 | 正文内容 |
| 下一节点 | 说完直接跳到哪个节点 | 填目标节点 ID。**和"玩家选项"二选一** |
| 玩家选项 | 给玩家几个选择 | 每个选项：选项文字 + 跳到哪个节点。**和"下一节点"二选一** |
| 效果 | 到达这个节点时产生的效果 | 和行动的效果规则一样 |
| 表情 | 说话时的表情 | 默认 `neutral`，可选表情看策划设定 |

### 核心规则

1. **每个对话必须有 `start` 和 `end` 节点**
2. `start` 的文本必须是空的（它只是标记入口）
3. `end` 的文本可以留空（它只是标记结束）
4. 「下一节点」和「玩家选项」**只能填一个**——要么直接跳到下一句，要么给玩家选择
5. 两个都不填的话，对话会卡住

### 详细操作步骤

**新建一个对话**：

1. 确保你在「💬 对话」标签页
2. 在左侧对话列表顶部点「**+ 新增对话**」
3. 输入对话 ID：`npc_小明`（英文下划线格式）
4. 确定后右侧出现节点编辑区，自动创建了 `start` 和 `end` 两个默认节点

**添加 NPC 说的第一句话**：

1. 点「**+ 新增节点**」
2. 节点 ID 填 `node_1`
3. 说话人填 `小明`
4. 文本填你要说的内容，比如「嗨！最近怎么样？」
5. 下一节点填 `end`（说完直接结束）

**把 start 连到这句话**：

1. 在节点列表中找到 `start` 那一行，点「编辑」
2. 下一节点填 `node_1`
3. 保存节点

**添加玩家选项**：

1. 点「+ 新增节点」
2. 节点 ID 填 `node_2`
3. 说话人填 `小明`
4. 文本填「你有什么想聊的？」
5. 点「+ 玩家选项」
	- 选项文字：「聊聊演出」→ 跳到 `node_3`
	- 选项文字：「聊聊天气」→ 跳到 `node_4`
6. 保存节点

**删除整棵对话树**：
- 在左侧对话列表中，点对话名称右边的 **✕** 按钮，确认后删除。

---

## 六、📱 手机消息 — 游戏里的聊天

### 什么是手机消息

模拟玩家在游戏里打开手机看到的聊天记录。每个"会话"就是一个聊天窗口。

### 界面布局

```
┌─ 左侧：会话列表 ─────┐  ┌─ 右侧：消息编辑区 ────────┐
│                      │  │                            │
│  band_group (群聊)    │  │  消息列表（表格）           │
│  xiao_ming (私聊)     │  │  ├─ msg_1: "大家好"       │
│                      │  │  ├─ msg_2: "明天排练吗"    │
│  [+ 新增]            │  │                            │
│                      │  │  [+ 新增消息]              │
│                      │  │  消息编辑面板               │
└──────────────────────┘  └────────────────────────────┘
```

### 会话字段

| 字段 | 意思 | 选项 |
|------|------|------|
| chat_id | 程序代号 | 英文下划线，如 `band_group` |
| chat_name | 聊天窗口显示的名字 | 如「乐队群」 |
| type | 群聊还是私聊 | `group`（群聊）或 `private`（私聊） |

### 消息字段

| 字段 | 意思 | 怎么填 |
|------|------|--------|
| id | 消息 ID | 英文下划线，如 `msg_1` |
| sender | 发消息的人的名字 | 如「小明」 |
| text | 消息内容 | 正文 |

### 消息发送时机（二选一）

每条消息需要指定它**什么时候出现在玩家手机里**。下面两种方式**选一种**，不能同时填：

| 方式 | 什么意思 | 填什么 |
|------|---------|--------|
| **定时发送** | 到第 X 天的第 Y 个时段自动出现 | `delay_day`（第几天）+ `delay_phase`（哪个时段） |
| **事件触发** | 某个事件发生后出现 | `trigger_event`（事件 ID） |

**举例**：
- "第 1 天早晨，小明发来问候" → delay_day=1, delay_phase=早晨
- "房东催租事件发生后，房东发来微信" → trigger_event=landlord_rent

### 详细操作步骤

1. 在「📱 手机消息」标签页，点「**+ 新增**」
2. 输入会话 ID，比如 `band_group`
3. 点左侧会话列表中的那一行，进入消息编辑
4. 在会话信息区修改名称和类型
5. 点「**+ 新增消息**」
6. 填写 sender（发消息的人）、text（内容）、选择发送方式
7. 点「💾 保存到数据库」

---

## 七、🌍 世界 — 游戏规则与配置

这个标签页管理的是**游戏本身的架构**，而不是内容。它有四个分节，顶部有切换按钮：

```
[⚙ 游戏参数]  [🗺 地图]  [📍 地点]  [👤 NPC]
```

### 数据之间的关系

**先创建地图，再创建地点和 NPC。**

```
地图（maps）
  └─ 地点（locations）— 每个地点属于一张地图
  └─ NPC（npcs）— 每个 NPC 属于一张地图
```

当你创建地点或 NPC 时，需要从下拉菜单中选择它属于哪张地图。

---

### ⚙ 游戏参数

控制游戏的数值规则。

| 字段 | 意思 | 默认值 | 什么时候改 |
|------|------|--------|----------|
| 一周天数 | 游戏里一周有几天 | 7 | 调整游戏节奏 |
| 初始金钱 | 玩家新游戏开始时有多少钱 | 500 | 调整难度 |
| 属性下限 | 属性最低能降到多少 | 0 | 一般不改 |
| 属性上限 | 属性最高能到多少 | 100 | 扩展成长空间 |
| 初始地点 | 玩家出生在哪个地点 | 出租屋 | 改开局位置 |
| 初始 X/Y 坐标 | 出生在地图上的精确位置 | (100, 240) | 微调开局站位 |
| 6 项初始属性值 | 每项属性的初始数值 | 演奏力 15、创作力 20… | 调整初始强度 |

**怎么改**：打开这个分节 → 改数字 → 点「💾 保存到数据库」

> **常用示例**：想把游戏缩短为 5 天 → 改「一周天数」为 5 → 保存 → 同步 → 重启游戏。

---

### 🗺 地图

管理游戏中的地图（例如老城区、新城区）。每张地图是一片独立的可探索区域。

#### 地图列表

表格显示每张地图的基本信息：

| 列 | 意思 | 示例 |
|----|------|------|
| ID | 程序代号 | `old_town` |
| 名称 | 游戏里显示的名字 | `老城区` |
| 场景路径 | Godot 场景文件路径 | `res://scenes/maps/old_town.tscn` |
| 尺寸 | 地图宽 × 高（像素） | `960 × 270` |

#### 编辑地图详情

点某一行展开编辑面板，包含以下字段：

**基本信息**：

| 字段 | 意思 | 怎么填 |
|------|------|--------|
| ID | 程序代号 | 英文下划线，如 `new_town` |
| 名称 | 显示名称 | 中文，如「新城区」 |
| 场景路径 | Godot 场景文件 | `res://scenes/maps/新文件名.tscn` |
| 宽度 | 地图宽度（像素） | 数字，默认 960 |
| 高度 | 地图高度（像素） | 数字，默认 270 |
| 地面 Y | 地面线在第几行像素 | 数字，默认 240（画面底部附近） |
| 地面高度 | 地面厚多少像素 | 数字，默认 30 |

**颜色**（每个颜色用 R/G/B 三个数字，范围 0~1）：

| 颜色 | 控制什么 | 默认值（R, G, B） |
|------|---------|-----------------|
| 天空色 | 地面以上的背景色 | (0.12, 0.15, 0.22) 深蓝黑 |
| 地面色 | 地面颜色 | (0.25, 0.18, 0.12) 棕色 |
| 地面线色 | 天空和地面的分界线 | (0.35, 0.28, 0.2) 浅棕 |
| 建筑色 | 建筑方块的颜色 | (0.18, 0.18, 0.2) 深灰 |
| 窗户色 | 建筑上小窗户的颜色 | (1.0, 0.9, 0.4, 0.3) 黄色半透明 |

> **提示**：颜色值可以用 0~1 之间的小数。如果你不确定填什么，先用默认值，在 Godot 里看了效果再回来微调。

**装饰建筑**：

装饰建筑是地图上只起装饰作用的方块（没有对应地点）。每行 4 个数字：
- X：方块左上角的 X 坐标
- Y：方块左上角的 Y 坐标
- W：方块的宽度
- H：方块的高度

点「**+ 添加装饰建筑**」增加一行，点 ✕ 删除一行。

#### 新增地图的完整流程

1. 打开「🌍 世界」→「🗺 地图」分节
2. 点「**+ 新增地图**」
3. 输入地图 ID（英文下划线，如 `university_gate`）
4. 表格中出现新行，点击它展开编辑面板
5. 填写名称、场景路径、尺寸
6. 调整颜色（或直接用默认值）
7. 点「**💾 保存到数据库**」
8. 在 Godot 中复制 `old_town.tscn` → 重命名为你填的场景路径中的文件名
9. 运行 `一键同步.bat` → 重启 Godot → 新地图可用

---

### 📍 地点

管理游戏中的所有场景（进入室内后的画面）。每个地点**属于一张地图**。

#### 字段说明

| 字段 | 意思 | 怎么填 |
|------|------|--------|
| ID | 程序代号 | 英文下划线，如 `studio` |
| 名称 | 游戏里显示的名字 | 中文，如「录音棚」 |
| **所属地图** | 这个地点在哪张地图上 | **下拉菜单选**（先创建地图） |
| 场景路径 | Godot 场景文件路径 | `res://scenes/locations/xxx.tscn` |
| 触发 X/Y | 玩家在地图上走到哪个位置弹出"按 E 进入" | 数字坐标 |
| 建筑 X/Y/W/H | 这个地点在地图上对应的建筑方块 | 四个数字：位置和大小 |
| 标签 X/Y | 建筑名字显示在地图上的位置 | 两个数字：标签坐标 |

#### 触发坐标和建筑方块的关系

```
地图画面：
      标签 (标签X, 标签Y) ← 显示地点名字
      ┌──────────────┐
      │   建筑方块    │ ← 建筑X, 建筑Y, 建筑W, 建筑H
      │  ┌────────┐  │
      │  │  入口  │  │ ← 触发X, 触发Y（玩家走到这里弹出提示）
      │  └────────┘  │
      └──────────────┘
```

- **建筑方块**是地图上画的装饰矩形
- **触发坐标**是玩家能按 E 进入的交互区域（通常在建筑下方，靠近地面线）
- **标签坐标**是地点名字显示的位置（通常在建筑上方）

#### 详细操作步骤

1. 打开「🌍 世界」→「📍 地点」分节
2. 点「**+ 新增地点**」
3. 输入地点 ID（英文下划线）
4. 点表格中新出现的那一行，展开编辑面板
5. 填写名称
6. 从**「所属地图」下拉菜单**中选择这个地点属于哪张地图
7. 填写场景路径（需要程序员先在 Godot 里创建场景文件）
8. 填写坐标（触发位置、建筑位置、标签位置）
9. 点「**💾 保存到数据库**」

添加新地点后，它会自动出现在「⚡ 行动」标签页的地点下拉菜单里。

---

### 👤 NPC

管理游戏中的所有角色。每个 NPC **属于一张地图**。

#### 字段说明

| 字段 | 意思 | 怎么填 |
|------|------|--------|
| ID | 程序代号 | 英文下划线，如 `npc_xiao_ming` |
| 名称 | 游戏里显示的名字 | 中文，如「小明」 |
| **所属地图** | 这个 NPC 在哪张地图上出现 | **下拉菜单选**（先创建地图） |
| 对话 ID | 关联哪个对话文件 | 如 `npc_小明`（在「💬 对话」标签页创建的） |
| 地图 X/Y | NPC 在地图上的位置 | 两个数字坐标 |

#### 重要提醒

- 创建 NPC 后，**务必在「💬 对话」标签页里为他创建一个对话文件**，对话 ID 要跟 NPC 的「对话 ID」字段一致
- 如果没有关联对话，游戏里这个 NPC 会是一个不会说话的木桩

#### 详细操作步骤

1. 打开「🌍 世界」→「👤 NPC」分节
2. 点「**+ 新增 NPC**」
3. 输入 NPC ID（如 `npc_drummer`）
4. 点表格中新出现的那一行，展开编辑面板
5. 填写名称（如「鼓手阿强」）
6. 从**「所属地图」下拉菜单**中选择这个 NPC 属于哪张地图
7. 填写对话 ID（如 `npc_drummer`）
8. 填写地图 X/Y 坐标
9. 点「**💾 保存到数据库**」
10. 切换到「💬 对话」标签页，为这个 NPC 创建对话文件

添加新 NPC 后，它会自动出现在「⚡ 行动」和「📋 事件」标签页的关系系统下拉菜单里。

---

## 八、怎么保存 & 怎么同步到游戏

### 保存

> **⚠️ 最重要的一条规则：改完必须点「💾 保存到数据库」按钮！没点保存，修改全部丢失！**

- 保存成功 → 左下角弹出绿色提示「已保存」
- 保存失败 → 弹出红色提示，检查网络后重试

**所有编辑面板都有保存按钮**，在表单最下方，蓝色背景。

**不想保存** → 点旁边的「取消」按钮，刚才的修改全部丢弃。

### 数据怎么到游戏里

```
你在编辑器填表 → 点保存 → 存到云端数据库（Supabase）
                                    │
                    运行 一键同步.bat（先预览，再确认写入）
                                    │
                    所有 JSON 文件更新（assets/json/ 目录）
                                    │
                    打开 Godot → 运行游戏
                                    │
                    你填的内容出现在游戏里 ✨
```

### 如何运行同步

**方式一（最简单）**：在项目目录下双击 **`一键同步.bat`**

新版一键同步会分两步：

1. 先输入团队编辑口令。
2. 脚本先做预览，只显示会改哪些内容，不修改本地文件。
3. 如果没有红色健康错误，确认要同步时输入 `YES`。
4. 脚本会先自动备份旧的本地 JSON，再覆盖 `assets/json/`。
5. 同步完成后，重启 Godot 查看更新。

如果预览阶段出现红色健康错误，脚本会停止，不会修改本地文件。

**方式二（命令行）**：
```
node 内容工具/online/sync_to_game.js --dry-run
node 内容工具/online/sync_to_game.js --yes
```

同步成功后你会看到变更摘要：
```
📊 变更摘要:
  行动  8(±0)      ← 未变化
  事件  8(±0)
  对话  13(±0)
  手机  4(±0)
  地图  1(±0)
  地点  5(±0)
  NPC   5(±0)
  游戏配置  未变化
```

> **"±0"** 的意思是没有变化。「+2」表示新增了 2 个，「~1」表示修改了 1 个，「-1」表示删除了 1 个。

---

## 九、多人协作注意事项

### 谁在线

左侧边栏的「在线成员」区域显示当前在编辑器里的人。桌面端显示完整名字列表，手机端点底部圆点可以展开。

### 修改冲突

- **编辑不同内容** → 互不影响，各改各的
- **同时编辑同一个条目** → 后保存的人覆盖先保存的人
- 别人保存后，你的页面会弹出提示「📡 XXX 更新了…」
- 如果收到这个提示时你正在编辑表单，建议**先保存你的修改**，再点「🔄 从数据库拉取」看最新的内容

### 切换标签页时的提醒

如果你正在编辑表单（展开了编辑面板），切换到另一个标签页时会弹出提示「你有未保存的编辑，确定离开吗？」。这是保护你免于丢失修改。

---

## 十、参考速查表

### 6 个属性

| 属性 |
|------|
| 演奏力 |
| 创作力 |
| 社交力 |
| 魅力 |
| 执行力 |
| 精神状态 |

### 4 个时段

早晨 → 下午 → 傍晚 → 深夜

（一天过完 4 个时段后进入下一天）

### 6 个运算符

| 符号 | 意思 |
|------|------|
| `>=` | 大于等于 |
| `<=` | 小于等于 |
| `>` | 大于 |
| `<` | 小于 |
| `==` | 等于 |
| `!=` | 不等于 |

### 5 种事件触发类型

| 英文 | 中文意思 |
|------|---------|
| `phase_start` | 进入时段时 |
| `action_complete` | 完成行动后 |
| `location` | 进入地点时 |
| `stat_threshold` | 属性达标时 |
| `week_end` | 一周结束时 |

---

## 十一、常见问题

### 保存相关

**Q: 我不小心关了页面，内容还在吗？**
A: 只要点了「💾 保存到数据库」就在。没点保存的话会丢失。养成随时保存的习惯。

**Q: 为什么点了保存没反应？**
A: 检查网络是否正常。如果一直保存失败，左下角会有红色提示。联系项目负责人检查 Supabase 状态。

### 编辑相关

**Q: 怎么删除一个行动/事件/地点/NPC/地图？**
A: 表格中每行最右边有红色的 **✕**，点一下会弹出确认框，确认后删除。

**Q: 怎么删除整棵对话树？**
A: 在「💬 对话」标签页左侧对话列表里，每个对话名称右边有 ✕。

**Q: 新增了 NPC，为什么在行动编辑器里选不到？**
A: 两件事要做：① 先在世界标签页保存 NPC；② 切换到行动标签页，下拉菜单已自动更新。如果还是看不到，刷新页面。

**Q: 新增了地图，为什么地点/NPC 的"所属地图"下拉里没有？**
A: 地图也要先保存。保存后下拉菜单会自动包含它。

**Q: 搜索过滤后点某一行，编辑的为什么不是我点的那一行？**
A: 这是之前的一个 bug，已经修复了。如果你还遇到这个问题，刷新页面试一下。

### 同步相关

**Q: 改了一周天数，游戏里为什么没变？**
A: 改 → 保存 → 运行「一键同步.bat」→ 重启 Godot 游戏。**三步缺一不可**。

**Q: 我新增了一个地点，玩家为什么进不去？**
A: 除了在编辑器里添加地点数据，还需要程序员在 Godot 里创建对应的场景文件（`.tscn` 文件）。编辑器管理的是**数据**，Godot 管理的是**画面和交互**。

**Q: 我新增了一张地图，游戏里能看到吗？**
A: 需要两步：① 编辑器保存地图数据 → 同步 JSON；② 程序员在 Godot 里复制一张现有的 `.tscn` 地图文件，改名为你在编辑器里填的场景路径。MapController 会自动读取 JSON 渲染。

### 手机相关

**Q: 手机能用吗？**
A: 能。浏览器打开同一个网址，界面会自动适配手机。底部的标签页选择框可以切换功能。

**Q: 手机上看不全表格怎么办？**
A: 横屏使用效果更好。表格可以左右滑动。

### 其他

**Q: 网页打不开？**
A: ① 检查网络；② 检查网址是不是 `online-omega-two.vercel.app`；③ 联系项目负责人。

**Q: 我该填什么内容？**
A: 内容设计由策划/文案负责。编辑器是**录入工具**，不负责内容创作。如果你不知道填什么，问策划要表。

**Q: 可以批量导入吗？**
A: 可以。每个标签页/分节都有「📥 导入」按钮，支持选择多个 `.json` 文件一次导入。

---

## 十二、完整操作流程示例

以下是在编辑器中完成一次完整提交流程的步骤：

### 场景：策划要加一张新地图"大学城"，配一个新地点"排练厅"和一个新 NPC "学姐"

**Step 1：创建地图**
1. 打开「🌍 世界」→「🗺 地图」
2. 点「+ 新增地图」，输入 ID：`university`
3. 点新出现的行 → 展开编辑面板
4. 名称填「大学城」
5. 场景路径填 `res://scenes/maps/university.tscn`
6. 尺寸、颜色用默认值
7. 点「💾 保存到数据库」

**Step 2：创建地点**
1. 切换到「📍 地点」
2. 点「+ 新增地点」，输入 ID：`uni_rehearsal`
3. 点新出现的行 → 展开编辑面板
4. 名称填「排练厅」
5. **所属地图**选「大学城」
6. 场景路径填 `res://scenes/locations/uni_rehearsal.tscn`
7. 坐标暂时用默认值（以后微调）
8. 点「💾 保存到数据库」

**Step 3：创建 NPC**
1. 切换到「👤 NPC」
2. 点「+ 新增 NPC」，输入 ID：`npc_senior`
3. 点新出现的行 → 展开编辑面板
4. 名称填「学姐」
5. **所属地图**选「大学城」
6. 对话 ID 填 `npc_senior`
7. 坐标暂时用默认值
8. 点「💾 保存到数据库」

**Step 4：创建 NPC 的对话**
1. 切换到「💬 对话」
2. 点「+ 新增对话」，输入 ID：`npc_senior`
3. 添加节点、写台词、设置分支……
4. 保存

**Step 5：为这个地点添加可执行的行动**
1. 切换到「⚡ 行动」
2. 点「+ 新增」，输入 ID：`practice_uni`
3. 名称填「大学排练」
4. **地点**下拉菜单选「排练厅」（刚创建的）
5. 设置条件和效果
6. 保存

**Step 6：同步到游戏**
1. 所有内容确认无误后，在桌面双击 `一键同步.bat`
2. 看到所有条目显示绿色 ✓ 和变更摘要
3. 程序员在 Godot 里创建对应的 `.tscn` 场景文件
4. 重启 Godot 游戏 → 新内容出现
